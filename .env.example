# Supabase project connection (see: supabase get_project_url / get_publishable_keys)
SUPABASE_URL=https://qrezyrtmeurkjbsysnxl.supabase.co
SUPABASE_PUBLISHABLE_KEY=
# Service role key: server-side only, never exposed to Lovable/front-end.
SUPABASE_SERVICE_ROLE_KEY=

# CORS: Lovable preview + published app origins
ALLOWED_ORIGINS=https://ai-m9e-vista.lovable.app,https://id-preview--e2f8ee4a-4881-4e30-92e2-a9a0605295ae.lovable.app

# Emma 01: normalization (Claude) + embeddings (OpenAI, 1536-dim to match ekg_entity.embedding)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Shared secret for trusted server-to-server callers (e.g. Lovable's server-side code,
# which has no Supabase user session for this project). Generate with:
#   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
INTERNAL_API_KEY=
